apiVersion: v1
kind: Service
metadata:
  name: {{ include "system-management-service.fullname" . }}
  labels:
    {{- include "system-management-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "system-management-service.selectorLabels" . | nindent 4 }}